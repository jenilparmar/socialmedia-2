// myContext.js
import React, { createContext } from 'react';

const ComentsContext =React.createContext(); // Renamed from `Coments` to `ComentsContext`

export default ComentsContext; // Export the context
